ext.archivesBaseName = "wover-event-api"
base {
    archivesName = "wover-event-api"
}

ext {
    baseNamespace = "event"
    baseClass = "WoverEvents"
}

moduleDependencies(project, ['wover-core-api'])
