ext.archivesBaseName = "wover-tag-api"
base {
    archivesName = "wover-tag-api"
}

ext {
    baseNamespace = "tag"
    baseClass = "WoverTag"
}

moduleDependencies(project, ['wover-core-api', 'wover-datagen-api', 'wover-event-api'])
//datagenDependencies(project, ['wover-preset-api'])
